# Core Flask and related packages
Flask==2.3.3
Werkzeug==2.3.7
Flask-Login==0.6.2
Flask-Mail==0.9.1

# Firebase and Google Cloud Firestore
firebase-admin==6.2.0
google-cloud-firestore==2.11.1

# Security and utilities
pyOpenSSL==23.2.0  # For secure email sending with smtplib if needed

# HTML to PDF generation (from /certificate route)
html2pdf  # Note: This might not be the exact package; see note below

# Development tools (optional, uncomment if needed)
# gunicorn==20.1.0  # For production deployment
